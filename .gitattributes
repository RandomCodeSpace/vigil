# VIGIL scripts: pin to LF line endings. The UTF-8 BOM is stored as literal
# bytes inside the files — do NOT use working-tree-encoding, that triggers
# git's iconv pass which fails on BOM round-trip.
*.ps1    -text
*.ps1xml -text
*.md     text eol=lf
*.json   text eol=lf
